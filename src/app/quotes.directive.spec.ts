import { Quotes.Directive } from './quotes.directive';

describe('Quotes.Directive', () => {
  it('should create an instance', () => {
    const directive = new QuotesDirective();
    expect(new Quotes.Directive()).toBeTruthy();
  });
});
